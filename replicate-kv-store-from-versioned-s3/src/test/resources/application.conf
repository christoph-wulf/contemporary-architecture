alpakka.s3 {
  # Points to localstack
  endpoint-url = "http://localhost:4572"
  # Localstack neither requires authentication nor a region
  aws {
    credentials.provider = anon
    region {
      provider = static
      default-region = ""
    }
  }
}